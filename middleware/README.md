# Cognira BTI Middleware

This top-level folder is kept for source-tree compatibility. Runtime orchestration lives in:

```text
backend/bti/middleware/pipeline_gateway.py
```

Cloud and API gateway integration adapters can be placed here when the hackathon modular monolith evolves to microservices.
